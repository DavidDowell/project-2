{{#each posts as |post|}}
  <li>
    {{> post-info post }}
  </li>
 {{/each}}

<button class="createPostBtn">
    <span>Create Post</span>
    <i class="bi bi-plus-lg"></i>
 </button>

<<<<<<< Updated upstream
<!-- Create Post Modal -->
<div class="postModal">

    <form class="modal-content">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title">
      <label for="content">Content:</label> 
      <textarea type="text" id="content" name="content"></textarea>
      <div class="btns">
          <button class="cancel">Cancel</button>
          <input class="submit" type="submit"></input>
       </div>
    </form>
  
  </div>

  <script src="js/add-post.js"></script>
=======
        {{#if posts.length}}
    <section>
    <h2 class="ml-3 mt-4">Your Posts</h2>
    <ul class="post-list d-flex flex-wrap justify-content-center">
        {{#each posts as |post|}}
        <li class="col-10 m-2">
            <div class="container col-12 prev-post p-2 m-2 d-flex align-items-center">
                <a href="/dashboard/comments/{{post.id}}" class="edit-link text-decoration-none text-light h4 col-10 p-1">{{post.title}}</a>
            </div>
        </li>
        {{/each}}
    </ul>
    </section>
        {{/if}}
>>>>>>> Stashed changes
