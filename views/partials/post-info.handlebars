   <div class="post">
            <a href="/comments/{{id}}" class="text-decoration-none h4 text-dark postTitle">{{title}}</a>
            <p class="postContent">{{post_text}}</p>
            <div class="postInfo"><span class="author">Moo</span> 10 min ago</div>
            <div class="likes">
                <i class="bi bi-hand-thumbs-up"><span class="voteCount">{{vote_count}}</span></i>
                <i class="bi bi-hand-thumbs-down"></i>
             </div>
            <button class="addCommentBtn">Add Comment</button>
            
            <form class="addCommentForm closed">
                <textarea type="text" class="commentTextArea" name="commentTextArea"></textarea>
                <label for="commentTextArea"></label>
                <div class="btns">
                    <button class="cancel">Cancel</button>
                    <input class="submit" type="submit"></input>
                </div>
            </form>

            <button class="viewCommentsBtn">{{comment.length}} Comments <div class="icon"><i class="bi bi-caret-down-fill"></div></i></button>


{{#if comments.length}}
<section>  
    {{#each comments as |comment|}}
      {{> comment}}    
    {{/each}}
</section>
{{/if}}       
     
 </div>